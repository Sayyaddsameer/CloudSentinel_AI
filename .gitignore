# Terraform — never commit state or secrets
terraform.tfstate
terraform.tfstate.backup
.terraform/
*.tfvars
*.auto.tfvars
.terraform.lock.hcl

# Lambda build artifacts
*.zip
package/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env
deploy.env
.venv/
venv/
*.egg-info/
dist/
build/

# Node (future tooling)
node_modules/

# OS
.DS_Store
Thumbs.db

# Security scan output
bandit-report.txt

# GCP service account key files — never commit
*-service-account*.json
*-key.json
gcp-*.json

# Coverage reports
.coverage
htmlcov/
coverage.xml

teardown.py
wire_frontend.py

# Frontend runtime config � auto-generated by deploy_console.py, never commit real values
# The placeholder version (env.js.example) is tracked instead
modules/frontend/js/env.js

# Vendored Lambda-layer dependencies (rebuilt from requirements; do not commit)
infrastructure/terraform/reporting-layer/python/
layer_tmp/

# Local deploy/build scripts and artefacts
scripts/
PRESENTATION_SCRIPT.md
Team053_CloudSentinel_AI_IEEE.docx

# Test output files
*_out.json
*_payload.json
fix_groq_env.py
invoke_policy.json

# Test output files
chat_test_out*.json
scan_out*.json
experiment_results*.json
*_latency_results.json
smoke.json

